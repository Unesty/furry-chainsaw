graphics = build_target('graphics',
	'graphics.c',
	target_type: 'executable',
	dependencies: [dep_dlg, dep_swa])

gameplay = build_target('gameplay',
	'gameplay.c',
	target_type: 'executable',
	dependencies: [])
